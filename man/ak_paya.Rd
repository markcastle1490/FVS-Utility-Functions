% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ak-equations.R
\name{ak_paya}
\alias{ak_paya}
\title{ak_paya}
\usage{
ak_paya(species, ht, age, si, type = 0, debug = FALSE)
}
\arguments{
\item{species:}{An integer or numeric vector of species codes (primarily 8, 11, or 12).
Invalid, missing (\code{NA}), or unrecognized codes automatically default to group
11.}

\item{ht:}{A numeric vector specifying the total tree height in feet. Used as an input
when type is 1 or when type is 0.}

\item{age:}{A numeric vector specifying the tree breast-height age in years. Used as an
input when type is 1 or 2.}

\item{si:}{A numeric vector specifying the site index baseline height in feet. Used as
an input when type is 2 or when type is 0.}

\item{type:}{An integer or numeric value specifying the calculation branch.

1 = Calculate site index from height

2 = Calculate height from site index

3 = Calculate age from site index and height}
}
\value{
A numeric vector representing either the calculated site index (in
feet), tree height (in feet), or tree age (in years) depending on the selected
calculation type.
}
\description{
Calculates site index from tree height and age, total tree height from site
index and age, or tree age from site index and height using the Paya
functional form from the FVS-AK variant.
}
